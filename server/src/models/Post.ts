import { createModel } from '../db/model';
export const Post = createModel<any>('posts', { defaults: { amountRaised: 0, images: [], severity: 'medium', isSurgery: false, surgeryDetails: { hospital: '', duration: '', sessions: 0, description: '' }, status: 'pending', viewCount: 0, likes: [], likesCount: 0, commentsCount: 0, donorsCount: 0, isActive: true, adminNotes: '', declineReason: '', milestonesReached: [], fundBreakdown: [], coOrganizers: [] }, references: { author: { collection: 'users' }, 'coOrganizers.user': { collection: 'users' } }, virtuals: d => ({ progress: d.targetAmount ? Math.min(100, Math.round((d.amountRaised / d.targetAmount) * 100)) : 0 }) });
export default Post;
